---
layout: layouts/site.njk
---
<main id="main" class="content-page">
  <section class="page-hero">
    <p class="kicker">{{ pageKicker | default('Резюме') }}</p>
    <h1>{{ pageTitle | default('Руководитель группы разработки на C#.') }}</h1>
    <div class="page-lead">
      <p class="lead">{{ pageLead }}</p>
      {% if pageLeadMore %}<p class="lead">{{ pageLeadMore }}</p>{% endif %}
    </div>
  </section>

  <section class="two-column">
    <article class="paper-card large">
      <h2>{{ profileTitle | default('Профиль') }}</h2>
      <ul class="clean-list">
        {% for item in profileItems %}
          <li>{{ item }}</li>
        {% endfor %}
      </ul>
    </article>
    <aside class="paper-card">
      <h2>{{ stackTitle | default('Основной стек') }}</h2>
      <div class="pill-cloud">
        {% for pill in stackPills %}
          <span>{{ pill }}</span>
        {% endfor %}
      </div>
      <p>{{ stackNote }}</p>
    </aside>
  </section>

  <section class="cards-three">
    {% for job in jobs %}
      <article class="paper-card">
        <h2>{{ job.name }}</h2>
        <p class="meta">{{ job.work_period }} · {{ job.post }}</p>
        <p>{{ job.descriptions[0].description }}</p>
      </article>
    {% endfor %}
    {% for item in edu %}
      <article class="paper-card">
        <h2>{{ item.name }}</h2>
        <p class="meta">{{ item.work_period }} · {{ item.post }}</p>
        <p>{{ item.descriptions[0].description }}</p>
        {% if item.descriptions[0].result %}
          <p>{{ item.descriptions[0].result }}</p>
        {% endif %}
      </article>
    {% endfor %}
  </section>
</main>
